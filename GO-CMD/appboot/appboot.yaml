desc: golang command line tool template
parameters:
  - key: ModuleGit
    type: string
    default: github.com/appboot
  - key: DockerGroup
    type: string
    default: appboot
scripts:
  after:
    - cd appboot && sh post.sh
