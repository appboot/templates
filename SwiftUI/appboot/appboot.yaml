desc: SwiftUI template
parameters:
  - key: OrgName
    type: string
    default: appboot
  - key: Object
    type: select
    options:
      - World
      - Swift
      - iOS
scripts:
  before:
    - echo before SwiftUI
  after:
    - cd appboot && sh after.sh
